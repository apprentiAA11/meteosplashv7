// ui/weather/rainFX.js

// Pour l’instant, la pluie est gérée par rainUI.
// Cette fonction sert de pont pour weatherUI.

export function applyRainFX(raw) {
  // volontairement vide
  // la pluie est déclenchée via rainUI + weatherState
}
