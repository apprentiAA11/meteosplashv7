/* ===========================
   core/radarForecastEngineV3.js
   MODE "PAST ONLY"
   Objectif : garder uniquement RainViewer (-2h → instant T)
   => aucun radar maison, aucun overlay canvas
=========================== */

export function initForecastRadarLayer(map) {
  // Radar futur désactivé
  return;
}

export function stopAnimation() {
  // Rien à stopper
}

export function clearForecastParticles() {
  // Rien à nettoyer
}

export function setBaseRadarTile(url) {
  // Plus utilisé
  return;
}
